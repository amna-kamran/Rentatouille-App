import 'package:flutter/material.dart';

class PropertyPageScreen extends StatelessWidget {
  const PropertyPageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
