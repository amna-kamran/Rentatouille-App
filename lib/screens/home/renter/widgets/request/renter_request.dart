import 'package:flutter/material.dart';

class RenterRequestScreen extends StatelessWidget {
  const RenterRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text("Renter req"),
        ),
      ),
    );
  }
}
